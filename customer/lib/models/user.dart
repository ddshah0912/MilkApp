class User {
  final String uid;
  final String firstname,lastname;
  final String contact, houseNo, landmark, city, pincode, country;
  User({ this.uid, this.firstname, this.lastname, this.contact, this.houseNo, this.landmark, this.city, this.pincode,this.country});
}